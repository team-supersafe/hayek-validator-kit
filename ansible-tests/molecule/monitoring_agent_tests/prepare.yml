---
- name: Prepare
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Ensure remote tmp directory exists
      ansible.builtin.raw: mkdir -p /tmp/.ansible/tmp && chmod 1777 /tmp /tmp/.ansible /tmp/.ansible/tmp

    - name: Update apt cache
      ansible.builtin.raw: apt update -y

    - name: Install base packages for monitoring_agent tests
      ansible.builtin.raw: apt install -y sudo curl ca-certificates gnupg python3 python3-apt
