---
- name: Generate final test report
  ansible.builtin.debug:
    msg: |
      ========================================
      Rust Environment v2 Role Test Summary
      ========================================

      ✅ Installation Structure: Verified
      ✅ Environment Configuration: Verified
      ✅ Functionality Tests: Verified
      ✅ RBAC Integration: Verified

      Test Configuration:
      - cargo_home: {{ cargo_home }}
      - rustup_home: {{ rustup_home }}
      - Test users from CSV: {{ (test_users | default({})) | dict2items | map(attribute='key') | list }}

      All tests completed successfully!
      Rust environment is fully functional and RBAC-integrated.
      ========================================
