---
- name: Verify RBAC and least-privilege (Modular Version)
  hosts: all
  gather_facts: false
  become: true
  tasks:
    # ========================================
    # MODULAR RBAC VERIFICATION SYSTEM
    # ========================================
    # All verification tasks are orchestrated through main.yml
    # which provides proper sequencing and organization
    
    - name: "�️ Execute Complete RBAC Security Verification"
      include_tasks: tasks/main.yml
