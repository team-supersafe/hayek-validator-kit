---
argument_specs:
  main:
    short_description: "Install Solana CLI"
    author: "Hayek Validator Team"
    description:
      - "Installs Solana CLI tools on the target host"
      - "Supports installation from binaries or source"
      - "Configures environment variables and paths"
    options:
      agave_version:
        type: str
        required: true
        description: "Version of Agave/Solana CLI to install"
      override_build_method:
        type: bool
        required: false
        default: false
        description: "Override architecture-based build decision. When true, forces build from source regardless of architecture"
      use_official_repo:
        type: bool
        required: false
        default: false
        description: "Use official repository instead of forked repository when building from source"
