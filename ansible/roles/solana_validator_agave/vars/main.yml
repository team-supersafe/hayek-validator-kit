---
# Other variables are expected as input when calling this role 
# and checked during the role's precheck task

# For agave validator, the keyset source name is the same as validator name
keyset_source_name: "{{ validator_name }}"
