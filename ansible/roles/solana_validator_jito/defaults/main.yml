# Jito relayer role defaults
jito_relayer_installed_releases_dir: "{{ solana_user_dir }}/.local/share/jito-relayer/install/releases"
jito_relayer_install_dir: "{{ solana_user_dir }}/.local/share/jito-relayer/install/active_release"
jito_relayer_user: "{{ solana_user | default('sol') }}"
jito_relayer_block_engine_url: https://dallas.testnet.block-engine.jito.wtf
jito_relayer_metrics_config: host=http://metrics.jito.wtf:8086,db=relayer,u=relayer-operators,p=jito-relayer-write

