---
all:
  hosts:
    host-alpha:
      ansible_host: 172.25.0.11
      ansible_port: 22
    host-bravo:
      ansible_host: 172.25.0.12
      ansible_port: 22
    host-charlie:
      ansible_host: 172.25.0.13
      ansible_port: 22
    host-monitoring:
      ansible_host: 172.25.0.14
      ansible_port: 22

  children:
    # ───── City Grouping ─────
    city_dal:
      hosts:
        host-alpha:
        host-bravo:
        host-charlie:

    # ───── Network Grouping ─────
    solana:
      hosts:
        host-alpha:
        host-bravo:
        host-charlie:
        host-monitoring:

    # ───── Solana Cluster Grouping ─────
    solana_localnet:
      vars:
        iam_set_immutable: false
      hosts:
        host-alpha:
        host-bravo:
        host-charlie:
        host-monitoring:
