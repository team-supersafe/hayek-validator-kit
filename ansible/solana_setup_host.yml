all:
  hosts:
    hyk-edg-mia:
      ansible_host: 45.77.74.20
      ansible_port: 2522

  children:
    # ───── Data Center Grouping ─────
    dc_edgevana:
      hosts:
        hyk-edg-mia:

    # ───── City Grouping ─────
    city_miami:
      hosts:
        hyk-edg-mia:

    # ───── Network Grouping ─────
    solana:
      hosts:
        hyk-edg-mia:

    # ───── Solana Cluster Grouping ─────
    solana_mainnet:
      hosts:
        hyk-edg-mia:
